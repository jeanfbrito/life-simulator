(
    spawn_groups: [
        // 70 Rabbits
        SpawnGroup(
            species: "Rabbit",
            count: 70,
            names: ["Rabbit"],
            spawn_area: (
                center: (0, 0),
                search_radius: 40,
                max_attempts: 10,
            ),
            sex_sequence: Some([Male, Female]),
            messages: None,
        ),
        // 20 Deer
        SpawnGroup(
            species: "Deer",
            count: 20,
            names: ["Deer"],
            spawn_area: (
                center: (20, 20),
                search_radius: 30,
                max_attempts: 10,
            ),
            sex_sequence: Some([Male, Female]),
            messages: None,
        ),
        // 8 Wolves
        SpawnGroup(
            species: "Wolf",
            count: 8,
            names: ["Wolf"],
            spawn_area: (
                center: (-40, -40),
                search_radius: 30,
                max_attempts: 10,
            ),
            sex_sequence: Some([Male, Female]),
            messages: None,
        ),
        // 2 Foxes
        SpawnGroup(
            species: "Fox",
            count: 2,
            names: ["Fox"],
            spawn_area: (
                center: (40, -40),
                search_radius: 20,
                max_attempts: 10,
            ),
            sex_sequence: Some([Male, Female]),
            messages: None,
        ),
    ],
    settings: (
        verbose_logging: false,
        enable_spawning: true,
        completion_message: "✓ 100 entities spawned",
        post_spawn_messages: [],
    ),
)
